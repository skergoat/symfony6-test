{% extends 'base.html.twig' %}

{% block title %}MahlerDiscs{% endblock %}

{% block body %}
    <h1>MahlerDiscs</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ mahler_disc.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ mahler_disc.name }}</td>
            </tr>
            <tr>
                <th>Date_edit</th>
                <td>{{ mahler_disc.dateEdit ? mahler_disc.dateEdit|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_mahler_discs_index') }}">back to list</a>

    <a href="{{ path('app_mahler_discs_edit', {'id': mahler_disc.id}) }}">edit</a>

    {{ include('mahler_discs/_delete_form.html.twig') }}
{% endblock %}
